//下拉列表需要的数据
const selectData = {};
selectData.destinationPort = datas.destinationPort || [];



selectData.loadingPort = datas.loadingPort || [];

selectData.tradeState = datas.tradeState || [];

selectData.transportationMode = datas.transportationMode || [];

selectData.containerType = datas.containerType || [];

selectData.packaging = datas.packaging || [];

selectData.payment = datas.payment || [];

selectData.taxation = datas.taxation || [];

selectData.tradeMode = datas.tradeMode || [];

selectData.priceTerms = datas.priceTerms || [];

selectData.customerCategory = datas.customerCategory || [];



selectData.consignee = datas.consignee || [];

selectData.product = datas.product || [];

selectData.unit = datas.unit || [];

selectData.currency = datas.currency || [];

selectData.factory = datas.factory || [];

